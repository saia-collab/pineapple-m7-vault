SECURITY PLACEHOLDER — 2026-08-22

The imported notebook previously stored at this path contained a plaintext OpenAI API key and was unsafe to keep in a public repository. Its executable contents were removed from the current tree.

Required follow-up:

1. Revoke/rotate the exposed OpenAI project key.
2. If this notebook is still needed, restore a clean copy from its original trusted source.
3. Load credentials from the environment or the notebook platform's secret manager.
4. Never paste a replacement key into this file or Git.

The historical Git object remains accessible to repository administrators, so current-file removal is not credential revocation.

<!-- M7-FIREWALL-EXEMPT: security-reference -->
