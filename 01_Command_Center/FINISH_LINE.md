---
type: finish_line_checklist
status: active
last_updated: 2026-06-18
color_primary: "#1A365D"
color_secondary: "#FBC02D"
color_status: "#00BFFF"
---

# M7 — THE FINISH LINE (stop building, start shipping)

The OS is **built and verified** (9 skills, 351 templates, firewall green=0, Hermes wired,
MCP registered, engine live on :3000, daily auto-sync ready). What's left is small and
finite. Do these, then START MARKETING.

## ✅ Already done (per your Claude Code run)
Scaffold · GROUNDING · brand_firewall · scoring · factory · intake · server.js · dashboard ·
Atlas index · CATALOG · Hermes skills + daemon · models.json (64k) · MCP registered.

## 🟡 LEFT TO EXECUTE — 6 small steps (~20 min, one time)
1. **Open Obsidian** on the vault, enable **Local REST API** (toggle "non-encrypted HTTP" ON,
   port 27124). This is the ONE thing that fixes every "MCP offline" message. Leave it open.
2. **Double-click `REGISTER_DAILY_SYNC.bat`** → the OS self-updates daily at 9 AM. Done worrying about updates.
3. **Add real API keys** to `04_Tech_Lab\config\.env`: OpenRouter (model routing), ElevenLabs
   (voice). Add the Meta token ONLY when you're ready to advertise.
4. **Drop the EMAI vault in:** drag the extracted `EMAI Starter Vault` folder into
   `03_Knowledge_Mat\`. It instantly becomes part of THIS Obsidian vault (keep your existing
   `.obsidian` config — do NOT overwrite it). Next daily sync indexes it.
5. **Run `INGEST_AND_INDEX.bat`** once to file the EMAI vault + refresh the catalog.
6. **Freeze v1.0.** No new tools/playbooks for 30 days. The system already absorbs updates
   via the model fleet config + daily sync — you do NOT need to rebuild for every new AI model.

## 🚀 THEN — the actual business (week 1)
- Generate ONE campaign (1-3-12 / CPPA / Frisco) → review PAUSED drafts in `Outbox_Drafts\`.
- Launch the Local Fan ad set first ($250/week). Let the 1% Kill / 1.5% Scale rules work.
- Book CPPAs. That's the whole point — everything above just feeds this.

## Mindset
You spent 3 weeks building the factory. The factory doesn't make money — the products it
ships do. The hardest, highest-value move now is to stop adding and start selling.

Ko e hala 'o e fononga ko e faka'apa'apa.

<!-- M7-FIREWALL-EXEMPT: governance-reference -->
