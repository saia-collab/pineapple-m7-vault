# ⏸️ PARKED — the big site migration (NOT your priority right now)

These 5 files are a **33-page site migration + domain move + 301 redirects** plan.
An earlier AI agent built it and tried to run it — and it **over-reached**: it briefly
took the live homepage offline, then restored it. The site is fine now.

**You never asked for a migration. You asked for SEO + tracking + keywords to track.**

So this whole plan is **parked here on purpose.** Do not run it. Don't publish 33 pages,
don't move the domain, don't import redirects — none of it — until:
1. The simple weekly SEO habit (see `../M7_WHAT_YOU_NEED_SEO_TRACKING.md`) is a routine, AND
2. You *decide* you want the migration, with a human clicking each step and a backup first.

The migration isn't wrong — it's just **big, risky, and not now.** It'll wait here safely.
