{
  "mcpServers": {
    "notebooklm": {
      "command": "uv",
      "args": ["run", "notebooklm-mcp"],
      "description": "NotebookLM MCP — @scout research ingestion and source synthesis"
    },
    "hermes": {
      "command": "uv",
      "args": ["run", "hermes-mcp"],
      "description": "Hermes agent bridge — @scribe content generation and Outbox_Drafts routing"
    },
    "m7-command-center": {
      "type": "http",
      "url": "http://localhost:3939",
      "description": "M7 Command Center (server.js) — vault Kanban, memory, Outbox and brand firewall APIs",
      "endpoints": {
        "health":     "GET  /api/health",
        "kanban":     "GET  /api/kanban",
        "memory":     "GET  /api/memory",
        "filecontent":"GET  /api/filecontent",
        "outbox":     "GET  /api/outbox",
        "firewall":   "POST /api/firewall",
        "approve":    "POST /api/approve",
        "memory_log": "POST /api/memory/log"
      }
    }
  },
  "googleSheets": {
    "sheetId": "11n8Bm6fd1dz8EB9_jwGgU9UKlXmoR7Gz9GGiFYtbnpA",
    "description": "Campaign tracking and pipeline ledger"
  }
}
