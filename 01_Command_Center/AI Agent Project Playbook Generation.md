<h1>M7 COMMAND CENTER: CLAUDE PROJECT PLAYBOOK</h1><h3>Pineapple Contractors M7 System Consolidation &amp; Deployment</h3><p>This document serves as the master execution playbook and operational boundary for the <strong>M7 Command Center</strong>. It defines the unified architecture, maps current consolidation progress, sets rigid safety gates to prevent costly human and autonomous errors, and outlines a clear runbook for an AI Agent (e.g., Claude Code, Cursor, or OpenCode) to finish deployment.</p><h2>1. M7 SYSTEM ARCHITECTURE &amp; MAP</h2><p>The M7 system orchestrates high-performance regional contractor campaigns, combining local ingestion pipelines, compliance analysis, and ad creative drafting into a unified Obsidian vault workspace.</p><pre><code>[01_Command_Center] (Playbooks, Runbooks &amp; System Logs)
       │
[03_Knowledge_Mat] (Research &amp; raw assets) ────&gt; [00_Atlas] (Unified, deduped templates &amp; Index)
       │
[04_Tech_Lab] (AI Model Fleet, server.js, local databases, logs &amp; zips inbox)
       │
[Outbox_Drafts] (PAUSED creative &amp; copy drafts ready for human audit)
<br class="ProseMirror-trailingBreak"></code></pre><h3>Core Port Configuration Table</h3><table><tbody><tr><th><p>Port</p></th><th><p>Service</p></th><th><p>Purpose</p></th><th><p>Security</p></th></tr><tr><td><p><strong>3000</strong></p></td><td><p>Express Engine (<code>server.js</code>)</p></td><td><p>Hosts UI Dashboard, /api/metrics, /api/models, /api/hermes</p></td><td><p>Localhost-only, Node 22</p></td></tr><tr><td><p><strong>27124</strong></p></td><td><p>Obsidian Local REST API</p></td><td><p>HTTPS API &amp; MCP server integration with Vault</p></td><td><p>Bearer Token Auth</p></td></tr><tr><td><p><strong>27123</strong></p></td><td><p>Obsidian Local REST API (HTTP)</p></td><td><p>Fallback HTTP endpoint for local client development</p></td><td><p>Bearer Token Auth</p></td></tr><tr><td><p><strong>11434</strong></p></td><td><p>Ollama Local Instance</p></td><td><p>Serves <code>gemma4-pineapple</code> &amp; background LLM queries</p></td><td><p>Offline local server</p></td></tr></tbody></table><h2>2. THE PROGRESS MATRIX: WHAT IS DONE VS. PENDING</h2><p>This matrix inventories the entire system. Any AI Agent executing changes must verify their work against this master status list.</p><h3>✅ Phase 1 &amp; 2: Command Center &amp; Vault Foundation (100% Executed)</h3><ul><li><p><strong><code>GROUNDING.md</code></strong>: Implemented in Vault Root and redundant <code>03_Knowledge_Mat/</code> to serve as the immutable brand constitution.</p></li><li><p><strong><code>00_Atlas/INDEX.md</code></strong>: Automated indexing engine that maps and links all resources in real-time.</p></li><li><p><strong>Directory Scaffolding</strong>: Structured rooms <code>01_Command_Center</code>, <code>03_Knowledge_Mat</code>, <code>04_Tech_Lab</code>, <code>00_Atlas</code>, and <code>Outbox_Drafts</code> with correct <code>.gitignore</code> and local environment files.</p></li></ul><h3>✅ Phase 3: Ingestion &amp; Support Scripts (100% Executed)</h3><ul><li><p><strong><code>m7_fetch.py</code></strong>: Clean, script-stripping research harvester that stores data in raw research folders.</p></li><li><p><strong><code>m7_cleanup.py</code></strong>: Duplicate-detection daemon using MD5 hashes to keep files light and fast.</p></li><li><p><strong><code>m7_aggregate.py</code></strong>: Automated asset compiler that flattens raw materials into standard templates.</p></li><li><p><strong><code>m7_skill_intake.py</code></strong>: Autonomous classifier that scans, filters, and files zip-delivered operational workflows.</p></li></ul><h3>✅ Phase 4: Express Engine &amp; Dashboard UI (100% Executed)</h3><ul><li><p><strong><code>server.js</code></strong>: Port 3000 Node server managing routes, system telemetry, and agent hooks.</p></li><li><p><strong><code>OS_Dashboard.html</code></strong>: Premium live GUI featuring AI model health indicators, sparklines, and task executors.</p></li><li><p><strong><code>RUN_M7_DASHBOARD.bat</code></strong>: One-click startup routine that triggers dependency checks, starts the Express server, and loads the interface.</p></li></ul><h3>🟡 Human Checklist: One-Time System Calibration (Needs Your Execution)</h3><p>To link the dashboard with your physical system, run through these manual environment configurations:</p><ol><li><p><strong>Start Ollama</strong>: Run <code>ollama serve</code> in a Windows terminal, then fetch your brand-tuned model: <code>ollama pull gemma4-pineapple</code>.</p></li><li><p><strong>Enable Obsidian Plugins</strong>:</p><ul><li><p>Open Obsidian -&gt; Go to Community Plugins -&gt; Install &amp; Enable <strong>Local REST API</strong>.</p></li><li><p>Copy your unique <strong>API Key (Bearer Token)</strong> from the plugin settings page.</p></li><li><p>Under settings, toggle <strong>Enable non-encrypted (HTTP) server</strong> if your system struggles with local self-signed SSL certs.</p></li></ul></li><li><p><strong>Update Configs</strong>:</p><ul><li><p>Open <code>04_Tech_Lab/config/models.json</code>.</p></li><li><p>Verify that ports for Hermes, Antigravity, and NotebookLM match your active local daemons.</p></li></ul></li></ol><h2>3. THE "HUMAN-IN-THE-LOOP" (HITL) GUARDRAILS</h2><p>To eliminate runway spending, compliance audits must be run on a semi-autonomous basis. The system is structurally engineered with hard gates to protect budget, brand, and data.</p><pre><code> [AI Agent Fleet] ──&gt; Writes Draft ──&gt; [Outbox Shield (PAUSED)] ──&gt; [Human Audit] ──&gt; [Deploy]
<br class="ProseMirror-trailingBreak"></code></pre><h3>Rule 1: The Outbox Shield (Hard-coded Pause)</h3><ul><li><p><strong>Constraint</strong>: No AI agent or automated script has permission to directly change an ad status to <code>ACTIVE</code> or <code>PUBLISHED</code>.</p></li><li><p><strong>Implementation</strong>: Every ad campaign drafted by <code>m7_factory.py</code> or through the <code>/api/hermes</code> endpoint must be output with:</p><pre><code>"delivery_status": "PAUSED",
"budget_lock": true,
"audit_required": true
<br class="ProseMirror-trailingBreak"></code></pre></li><li><p><strong>Actionable Safe Gate</strong>: The live ad engines will refuse to ingest any document that does not contain a cryptographically matching <code>approved.json</code> key manually placed into <code>Outbox_Drafts/</code> by a human manager.</p></li></ul><h3>Rule 2: Brand Firewall Auto-Mutation</h3><ul><li><p><strong>Constraint</strong>: Direct translation of banned, high-liability regulatory keywords must be corrected before files are saved.</p></li><li><p><strong>Implementation</strong>: If a script or file intake notices non-compliant terms (such as "Complimentary" or unauthorized claims like "IKO Certified (RCAT License #03-0637)"), the <code>brand_firewall.py</code> hook automatically mutates the content in memory to standard, safe variations ("CPPA Compliant", "IKO Quality") and logs the incident:</p><pre><code>[FIREWALL EXCEPTION] Mutated keyword 'Complimentary' to safety wrapper 'CPPA Checked' in draft_04.json
<br class="ProseMirror-trailingBreak"></code></pre></li></ul><h3>Rule 3: Isolation of Live APIs</h3><ul><li><p><strong>Constraint</strong>: Financial API keys or direct payment gateways must never be injected into agent memory.</p></li><li><p><strong>Implementation</strong>: Claude and background scripts only edit localized text templates. They do not hold the runtime keys for payment platforms, search integrations, or ad console APIs. All publication routes must be manually executed from the protected host machine.</p></li></ul><h2>4. AI AGENT RUNBOOK (For Claude Code &amp; Local Agents)</h2><p>If you are running <strong>Claude Code</strong> (or another developer agent) to perform maintenance, refactoring, or to process telemetry files, copy and paste these exact instruction sets.</p><h3>AI Agent Grounding Prompt (Paste to Agent)</h3><pre><code>You are the M7 AI Agent working within the Pineapple Contractors Command Center.
Your goal is to optimize, monitor, or debug the M7 platform safely.
CRITICAL RULES:
1. Always keep the 'Outbox Shield' intact. Do not change default statuses from 'PAUSED' to 'ACTIVE'.
2. Always write and compile scripts in '04_Tech_Lab' or stable temporary folders before pushing to live production directories.
3. Run verification checks (e.g., node syntax, python compilation) after every single file edit.
4. If context space is full, run `/compact` to maintain performance.
<br class="ProseMirror-trailingBreak"></code></pre><h3>Typical Maintenance &amp; Operational Tasks</h3><h4>Task A: Verify express engine health and configurations</h4><ul><li><p><strong>Command</strong>: <code>node --check 04_Tech_Lab/server.js</code></p></li><li><p><strong>Command</strong>: <code>python -m py_compile 04_Tech_Lab/m7_factory.py</code></p></li><li><p><strong>Run Goal</strong>: Ensures that no syntax breaking occurred during automatic configurations.</p></li></ul><h4>Task B: Flush duplicate files and rebuild the Index</h4><ul><li><p><strong>Command</strong>: <code>python 04_Tech_Lab/m7_cleanup.py --apply</code></p></li><li><p><strong>Command</strong>: <code>python 04_Tech_Lab/m7_aggregate.py</code></p></li><li><p><strong>Agent Instructions</strong>:</p><ol><li><p>Scan <code>03_Knowledge_Mat/raw/</code> for structural redundancies.</p></li><li><p>Run deduplication checks.</p></li><li><p>Flat compile all unique items directly into <code>00_Atlas/</code>.</p></li><li><p>Automatically update the primary master map in <code>00_Atlas/INDEX.md</code>.</p></li></ol></li></ul><h4>Task C: Prompt Local Ollama Agent via API</h4><ul><li><p>To test if the local model is accessible via the Node engine:</p><pre><code>curl -X POST http://localhost:3000/api/ollama -H "Content-Type: application/json" -d "{\"prompt\": \"Analyze ad copy: Quality roofing starting today.\"}"
<br class="ProseMirror-trailingBreak"></code></pre></li></ul><h2>5. OBSIDIAN LOCAL REST API &amp; MCP CONFIGURATION</h2><p>To allow Claude Code or Cursor to read, write, and map your knowledge base in real-time using natural language, configure the vault as an <strong>MCP (Model Context Protocol)</strong> node.</p><h3>A. Claude Desktop Integration (Windows)</h3><p>Open your global config file at <code>%APPDATA%\Claude\claude_desktop_config.json</code> and insert the following block:</p><pre><code>{
  "mcpServers": {
    "obsidian": {
      "command": "npx",
      "args": [
        "mcp-remote@latest",
        "[https://127.0.0.1:27124/mcp/](https://127.0.0.1:27124/mcp/)",
        "--header",
        "Authorization: Bearer YOUR_OBSIDIAN_API_KEY"
      ]
    }
  }
}
<br class="ProseMirror-trailingBreak"></code></pre><p><em>(Make sure to replace <code>YOUR_OBSIDIAN_API_KEY</code> with the Bearer token copied from your Obsidian settings window).</em></p><h3>B. Claude Code CLI Setup</h3><p>To add your Obsidian vault directly to the Claude Code CLI, run the following command in your terminal:</p><pre><code>claude mcp add --transport http obsidian [https://127.0.0.1:27124/mcp/](https://127.0.0.1:27124/mcp/) --header "Authorization: Bearer YOUR_OBSIDIAN_API_KEY"
<br class="ProseMirror-trailingBreak"></code></pre><p>If you face SSL/Certificate issues due to local self-signed certificates, bypass the certificate rejection by running:</p><pre><code>set NODE_TLS_REJECT_UNAUTHORIZED=0
<br class="ProseMirror-trailingBreak"></code></pre><p>Then use the unencrypted HTTP port (if enabled in your Obsidian plugin settings):</p><pre><code>claude mcp add --transport http obsidian [http://127.0.0.1:27123/mcp/](http://127.0.0.1:27123/mcp/) --header "Authorization: Bearer YOUR_OBSIDIAN_API_KEY"
<br class="ProseMirror-trailingBreak"></code></pre><h2>6. ONE-CLICK SKILLS &amp; INTAKE PROTOCOL (TOMORROW'S PIPELINE)</h2><p>When your <strong>15 zip files</strong> arrive tomorrow, follow this flawless intake protocol to process, verify, and store them with zero human error.</p><pre><code> [15 Skill Zips] ──&gt; Drop into 'skills_inbox/' ──&gt; Double-click 'INGEST_SKILLS.bat' ──&gt; Complete
<br class="ProseMirror-trailingBreak"></code></pre><h3>Steps for Skill Intake:</h3><ol><li><p><strong>Placement</strong>: Paste all 15 downloaded <code>.zip</code> files directly into <code>04_Tech_Lab\skills_inbox\</code>.</p></li><li><p><strong>Execution</strong>: Double-click <code>04_Tech_Lab\INGEST_SKILLS.bat</code>.</p></li><li><p><strong>Autonomous Processing</strong>:</p><ul><li><p>The intake engine classifies the contents (Skills route to <code>04_Tech_Lab\skills\</code>; Templates route to <code>03_Knowledge_Mat\00_Atlas\templates\</code>).</p></li><li><p>The Brand Firewall automatically scans, checks against <code>GROUNDING.md</code>, and strips out any banned terms.</p></li><li><p>Files are cleanly extracted, indexed, and logged to <code>04_Tech_Lab\logs\intake_log.json</code>.</p></li></ul></li><li><p><strong>Verification Check</strong>:</p><ul><li><p>Open your dashboard at <code>http://localhost:3000/OS_Dashboard.html</code>.</p></li><li><p>Look at the "Console Log" output panel at the bottom.</p></li><li><p>Verify that the "Task &amp; Playbook Runner" panel displays cyan success states for the file intake.</p></li></ul></li></ol><h2>7. SYSTEM COVENANT &amp; PRINCIPLES</h2><ul><li><p><strong>The Single-Truth Rule</strong>: Everything in the command center must conform to <code>GROUNDING.md</code>.</p></li><li><p><strong>Zero-Loss Ingestion</strong>: Never overwrite original data. Scripts must append, timestamp, or move to archives rather than destroying active creative copies.</p></li><li><p><strong>Human Sovereignty</strong>: AI drafts everything, but a human must click publish. The final ad spend is the boundary of automation.</p></li></ul>