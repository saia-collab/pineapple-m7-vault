# /today

Build a daily plan from the live vault plus the compiled today prompt.

Read and execute the workflow file at `Machine/Workflows/today.md` — follow every step exactly as documented.

The user's arguments (if any) are: $ARGUMENTS
