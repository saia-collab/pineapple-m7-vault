# /interview

Personalize the vault so `/today` and `/closeday` work around the user's actual operating style.

Read and execute the workflow file at `Machine/Workflows/interview.md` — follow every step exactly as documented.

The user's arguments (if any) are: $ARGUMENTS
