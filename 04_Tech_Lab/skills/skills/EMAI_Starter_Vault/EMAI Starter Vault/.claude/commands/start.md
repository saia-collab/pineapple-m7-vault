# /start

Orient the user to the vault, the included commands, and the fastest next step.

Read and execute the workflow file at `Machine/Workflows/start.md` — follow every step exactly as documented.

The user's arguments (if any) are: $ARGUMENTS
